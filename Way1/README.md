# Way1 工作区

本目录是 **Way1 方案** 的独立工作区，对应 Git 分支 `way1`（从 `main` 分出）。

- 共享基础（`思路.md`、`offline_sim/`、题面与附件）来自 `main`，请勿在本目录外改动。
- **本方案的所有代码实现只写在 `Way1/` 目录内。**
- 提交到 `way1` 分支，推送至 `origin/way1`。

约定与 `Way2/` 一致：一个方案 = 一个 `way{N}` 分支 + 一个 `Way{N}/` 子目录。
