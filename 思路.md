# B 题《无线电干扰源的快速自动定位与清除》解题思路

> 本文只谈建模与算法思路、系统设计与测试方案，不含具体代码实现。
> 目的是在动手前把物理规则、数学模型、四问的解法、以及与模拟器交互的工程框架都想清楚。

---

## 0. 问题一句话概述

机器狗携带**测向机 + 光学/激光清除仪**，在半径 1800 m 的圆形区域（圆心为原点，x 正东、y 正北，单位 m）内，
对若干个**无线电干扰源**进行**定位并清除**。每个干扰源占用一个互不相同的频道（1–20），共 10–16 个，个数未知。
机器狗从 (0,0) 出发、测向机初始频道为 1，通过 HTTP+JSON 与模拟器通信（`/enter`、`/measure`、`/clear`、`/exit`），
在**保证全部清除**的前提下**使总（虚拟）时间尽量短**。

四个问题层层递进：

| 问题 | 性质 | 核心任务 |
|---|---|---|
| 问题 1 | 纯几何/算法 | 已知若干检测点坐标与对同一干扰源的示向度，求交会定位"多边形定位区域"的**直径**，并判断"以该直径为直径的圆能否覆盖此区域" |
| 问题 2 | 几何+误差分析 | 已知第一个检测点的示向度，给出**第二个检测点的选择策略**与**候选区域**，使定位效果更好 |
| 问题 3 | 系统+算法+实测 | 全部为**全向**干扰源时，设计搜索—定位—清除的完整策略与算法，接入模拟器实测并统计指标 |
| 问题 4 | 系统+算法+实测 | 同时存在**全向与定向**干扰源（定向个数、朝向均未知）时的检测与清除策略与算法 |

---

## 1. 从题面提炼的关键规则（建模前提）

把散落在正文、附录 1–4、附件 1、附件 2 里的规则集中整理如下，后面所有建模都以此为准。

### 1.1 物理规则
1. **频道**：整数 1–20，每个频道**至多一个**干扰源；一局干扰源总数 10–16，个数不通过接口返回，需自行推断。
2. **有效接收半径** `R_eff`：每个干扰源一个，取值区间 **[1000, 1500] m**，各源不同，**接口不返回**。超出该半径则收不到该源信号。
3. **全向 vs 定向**：
   - 全向：只要距离 ≤ `R_eff` 就能收到。
   - 定向：需**同时满足**距离 ≤ `R_eff` **且**检测点位于其有效覆盖角内。覆盖角为**以定向方向为中线、两侧各 90°（共 180°，含边界）的半平面**，方向未知。
   - 覆盖角内信号**均匀辐射**，场强**只随距离衰减、与角度无关** → 因此接口**不返回场强/RSSI**，只返回"示向度 / near / no_signal"。
4. **示向度（svd_deg）**：检测点 S 指向干扰源 G 的方位角（东=0°、逆时针为正、范围 [0,360)）。**含误差 ±1°**（保留两位小数）。
5. **示向度误差特性（关键）**：误差是**每个地点固定的系统偏差**，取值恒在 [−1°, 1°]。**同一地点重复测量误差不变**（重复测量无益），只有在**不同地点**测量误差才呈现统计规律。→ 想削弱误差必须用**空间上不同的检测点**，而非原地重复。
6. **近距离阈值 5 m**：检测点与干扰源距离 ≤ 5 m 且在覆盖角内 → 信号过强，返回 `near`，**无示向度**；此时可直接光学精定位并清除。
7. **清除半径 20 m**：清除点与目标源距离 ≤ 20 m 即可**成功清除**（无论全向/定向，与朝向无关）。同一源只能清除一次，重复清除返回"未发现"。
8. **运动**：直线行驶，速度 **5 m/s**；检测/清除时必须停下。忽略高度差。

### 1.2 计时模型（虚拟时间，均为被评分的时间）
| 动作 | 触发方式 | 虚拟耗时 |
|---|---|---|
| `/enter`、`/exit` | 独立指令 | 0 s（不推进虚拟钟）|
| 移动 | 由 `/measure`/`/clear` 的 `position` 推断 | 直线距离 / 5 (s) |
| 切换频道 | 由 `/measure` 的 `channel` 推断（`/clear` 的 channel **不切**）| 频道变则 1 s，否则 0 |
| 检测 `/measure` | 独立指令 | 5 s（+移动+切换）|
| 清除 `/clear` | 独立指令 | 未发现 3 s / 成功 5 s（+移动）|

- 单次 `/measure` 总耗时 = 移动 + 切换频道 + 5；单次 `/clear` 总耗时 = 移动 + (3 或 5)。
- `accepted=true` 才推进虚拟钟；`accepted=false` 时 `virtual_time_s=0`（**不能**当成当前时刻）。

### 1.3 约束与截止
- **现实时间**：`/enter` 成功后最长 20 min，且受 25 min 窗口约束，用 `remaining_real_duration_s`（0–1200 s）为准。→ 但 HTTP 往返极快、5 s 是虚拟耗时不需真实等待，故现实时间对合理策略**几乎不构成约束**。
- **虚拟时间**上限 360000 s（100 h）——非常宽松，实际不会触及。
- **坐标**：分量绝对值 ≤ 2×10⁶，有限数值；可提交区域外坐标（仍照常计费与判定）。
- **通信**：串行、逐个等待响应；每个新动作用**新 `request_id`**，仅"网络重试同一动作"时复用原 id 与原内容；需同时判 HTTP 状态与 `accepted`。

### 1.4 目标函数与评分
- **被清除比例** = 被清除个数 / 干扰源总数（希望 = 1）。
- **平均定位清除时间** = 定位清除总时间 / 被清除个数（越小越好）。
  - "总时间"含移动、切换、检测、光学精定位、清除时间，全部是**虚拟时间**。

> **结论**：优化目标 = 在**清除全部**（比例 = 1）的前提下**最小化总虚拟时间**。
> 虚拟时间里**移动**（长距离 = 大耗时，跨区 3600 m = 720 s）和**检测次数**（每次 5 s）是大头，切换（1 s）和清除（3/5 s）较小。

---

## 2. 统一数学模型

### 2.1 坐标、方位与"示向度楔形"
- 检测点 S = (Sx, Sy)，干扰源 G = (Gx, Gy)。真实方位角 α*(S) = atan2(Gy−Sy, Gx−Sx)，归一化到 [0,360)。
- 测得示向度 svd 满足 **svd − 1° ≤ α* ≤ svd + 1°**（误差有界确定）。
- 因此"G 相对 S 的可能方向"落在从 S 出发、**半张角 δ = 1°** 的**楔形（角锥）** `W(S, svd)` 内：
  ```
  W(S, svd) = { P : angle(P − S) ∈ [svd − 1°, svd + 1°] }
  ```
  这是**保证包含**真实 G 的区域（只要模型成立），是问题 1 的基石。

### 2.2 交会定位区域
多个检测点的楔形之交 `D = ∩_i W(S_i, svd_i)` 是一个**凸多边形**（可能无界/为空），即题目"多边形定位区域"。两个检测点时退化为图 2 的"红色四边形"。

### 2.3 误差 → 定位精度（供问题 2、3 使用）
两条视线在 G 处的**交会角** γ、到源距离 r1、r2 时，交会区域近似为平行四边形，**长对角线（直径）**约为
```
L ≈ (2δ / sinγ) · sqrt(r1² + r2² + 2 r1 r2 |cosγ|)
```
- 直观：视线越接近**垂直（γ=90°）**、到源**距离越近（r 越小）**，定位区域越小。γ→0 或 180°（近共线）时 L 爆炸。
- 这是问题 2"如何选第二点"和问题 3/4"何时停止测量、可否直接清"的定量依据。

### 2.4 代价（用于路径规划的离线代价模型）
把一条行动计划映射为总虚拟时间：
```
T = Σ 移动段(dist/5) + Σ 切换(1 · [频道改变]) + 5·N_measure + Σ clear(3或5)
```
我们会实现一个**与模拟器一致的离线代价/仿真器**，用于策略开发、参数调优与自测（见 §7、§8）。

---

## 3. 问题 1：定位区域直径与"直径圆"覆盖判定

### 3.1 建模
输入：检测点 S₁…Sₙ 及对**同一**干扰源的示向度 θ₁…θₙ（各 ±1°）。
输出：定位区域 D 的**直径** diam(D)=区域内两点最大距离；判断以 diam 为直径的圆能否覆盖 D。

### 3.2 求定位区域 D（半平面交）
每个楔形 = 两个半平面之交（左边界方向 θᵢ+1°、右边界方向 θᵢ−1°）：
```
以 S_i 为顶点、单位方向 u_lo=dir(θ_i−1°), u_hi=dir(θ_i+1°)
半平面A:  cross(u_lo, P − S_i) ≥ 0
半平面B:  cross(u_hi, P − S_i) ≤ 0     // 选取 <180° 的内侧楔形
```
- D = 2n 个半平面之交，用**半平面交**算法（O(n log n)）或**逐半平面裁剪**（用一个大初始凸多边形，例如目标圆的外接正多边形或包围盒，依次裁剪）求出凸多边形顶点集。
- 边界情形：
  - **为空** → 测量相互矛盾（误差超模型/并非同一源），报告无一致区域。
  - **无界**（n=1 或所有视线近平行）→ 用**目标圆 R=1800** 裁剪，得到有界区域（因为 G 一定在区域内）。

### 3.3 直径
凸多边形直径 = **某两个顶点**之间的距离最大值。用**旋转卡壳**（O(V)）或顶点对暴力（O(V²)，V 很小时足够）求出直径对 (A, B) 与 diam=|AB|。

### 3.4 "直径圆"能否覆盖 D —— 判定与结论
设直径圆 C：圆心 M=(A+B)/2，半径 = diam/2。由 Thales 定理：
> 点 P 在以 AB 为直径的圆内（含边界） ⟺ ∠APB ≥ 90° ⟺ (P−A)·(P−B) ≤ 0。

**判定算法**：对 D 的每个顶点 P 检查 (P−A)·(P−B) ≤ 0。
- 全部满足 → **能覆盖**（此时最小包围圆就是直径圆）。
- 存在某 P 不满足 → **不能覆盖**（存在顶点在圆外）。

**一般性结论**：并非任意"直径圆"都能覆盖凸集（经典反例：等边三角形，直径=边长 D，最小包围圆半径 = D/√3 ≈ 0.577D > D/2）。由 Jung 定理，直径为 D 的平面集合的最小包围圆半径可达 D/√3，一般**大于** D/2。
但对交会定位得到的**细长四边形/多边形**（典型形态，见图 2），直径就是长对角线，另两个顶点从侧面看该对角线的张角为**钝角** → 满足 ∠APB≥90° → **能覆盖**。

**交付**：给出 (1) 半平面交求 D 的算法伪代码；(2) 旋转卡壳求直径；(3) 上述覆盖判定与充要条件；(4) 用问题给定/自造的检测点数据画图演示（细长区域 → 覆盖成立；构造一个近等边的反例 → 覆盖不成立），并给出复杂度 O(n log n)。

---

## 4. 问题 2：第二个检测点的选择策略与候选区域

### 4.1 目标
已知第一个检测点 S₁ 与示向度 θ₁（±1°，真源在楔形视线 ℓ₁ 上，距离 r₁ 未知）。选第二点 S₂ 使**交会定位区域最小**（即 §2.3 的 L 最小），且 S₂ **确实能收到该源信号**。

### 4.2 关键结论（由 §2.3 推导）
`L ∝ 1/sinγ · sqrt(r₁²+r₂²+2r₁r₂|cosγ|)`，最小化要求：
1. **交会角 γ ≈ 90°**（视线在源处尽量垂直）—— 主导因素；
2. **r₂ 尽量小**（第二点尽量靠近源，但 r₂>5 m 且 ≤ 有效接收半径）。

因为 r₁ 固定不可控，L 有下界 ≈ 2δ·r₁；所以"垂直 + 靠近"能把区域压到最小。

### 4.3 几何构造（S₂ 的选取）
以 S₁ 为原点、沿视线方向 û=dir(θ₁)、法向 n̂=dir(θ₁+90°)。设 G = S₁ + r₁û。取
```
S₂ = S₁ + s·û + h·n̂
```
- 交会角 γ 满足 `cosγ ∝ (r₁ − s)`；**γ=90° ⟺ s = r₁**（S₂ 与源"平齐"），此时 r₂=|h|。
- 由于 r₁ 未知 → S₂ 不是一个点而是一个**候选区域**：对 r₁ 的合理取值范围 [r_min, r_max] 扫描，得到视线两侧对称的两片"带状/月牙"区域。

### 4.4 候选区域（给出显式描述）
```
候选区域 = { S₂ = S₁ + s·û + h·n̂ :
             s ∈ [r_min, r_max]        // 沿视线覆盖源可能的纵深
             |h| ∈ [h_min, h_max]      // 垂直偏移，控制 γ 与 r₂
             且满足下列约束 }
约束：
  (a) 交会角 γ(S₂; 该纵深) ∈ 良好带，如 [60°,120°]（sinγ ≥ 0.87）；
  (b) 可收到信号：r₂ = |S₂−G| ≤ 保守接收半径（取 1000 m 更稳）；
  (c) 避开近区且留裕度：r₂ > 5 m（一般取 ≥ 数十米）；
  (d) 基线 |S₁S₂| 足够大以形成有效视差；
  (e) 可达性：S₂ 落在可行活动范围（一般取目标圆内）。
```
形状：关于视线 ℓ₁ 对称的**两片弯月形带**（视线左右各一），位于"源的两侧、与源大致平齐、且在接收范围内"的位置。

### 4.5 推荐单点（工程用）
取源距离的名义值 r̂₁（如 [r_min,r_max] 中点，或用视线在圆内的弦长估计），令 `s = r̂₁`、`|h| = r̂₁`（→ γ≈90°, r₂≈r̂₁），得
```
S₂* = S₁ + r̂₁·û ± r̂₁·n̂   （左右两选一，选更靠区域中心/更易到达的一侧）
```
再按约束 (b)(c) 收缩 |h| 以保证收到信号。若 S₂ 实测 `no_signal`（超范围/在定向盲区），**回退**：沿 ℓ₁ 向源方向前移后再垂直偏移，重试。

**交付**：误差—交会角关系推导 + L 公式 + 候选区域图（两片弯月带）+ 推荐点构造 + 失败回退策略。

---

## 5. 问题 3：全向干扰源的搜索—定位—清除

### 5.1 总体框架（粗到精、边走边定位）
由 §1.4，优化 = 清除全部 + 最短虚拟时间；移动与检测是大头。整体分三段：

```
A. 发现扫描（Discovery）：在少量能覆盖全盘的观测点上扫频，得到"哪些频道有源 + 初始示向度"
B. 粗定位 + 排线（Localize & Route）：对每个活跃频道用 ≥2 个示向度交会出粗略位置；规划访问顺序（近似 TSP）
C. 逐个精定位并清除（Endgame）：沿途接近目标，用短程测量把误差压到 <20 m，/clear 成功即完成
```
核心利器是**20 m 清除半径**：定位精度只需 < 20 m，无需毫米级，这给了很大裕度。

### 5.2 发现扫描：覆盖点设计（保证比例=1 的关键）
- 有效接收半径 ≥ 1000 → 只要每个源在**某观测点 1000 m 内**就必被发现。
- 用**半径 1000 的圆覆盖半径 1800 的盘**：取 **中心 (0,0) + 半径约 1500 处 6 个方向点** 共 **7 点**即可单重覆盖（最远盘上点到最近观测点 ≈ 900 m < 1000，验证通过）。
- 在每个观测点扫**尚未清除**的频道；一次全频扫描 = 20×5 + 19×1 ≈ 119 s（会随已清频道减少而变快）。
- **重叠红利**：接收半径大，很多源会被多个观测点看到 → 自然获得 ≥2 个示向度用于交会，省去额外测量。

### 5.3 定位：多示向度最小二乘三角定位
对某频道收集到的 {(Sᵢ, θᵢ)}：每条视线给出过 Sᵢ、法向 nᵢ=dir(θᵢ+90°) 的直线约束 `nᵢ·(G−Sᵢ)=0`。叠成超定线性方程 `A·G = b`，**加权最小二乘**（近距离测量权重更大，因绝对误差更小）解出 Ĝ，并由残差/协方差估计定位不确定度：
- 若不确定度 < 20 m（且几何良好）→ 直接前往 Ĝ 清除；
- 否则按问题 2 策略补测一个"垂直、更近"的点，或留待 Endgame 就地精定位。

同时用问题 1 的**楔形交**给出**保证包含**真源的区域（作为稳健上界，指导是否需要补测）。

### 5.4 Endgame：利用 20 m 裕度的稳健收尾
到达估计点附近后：
```
1. 若曾返回 near（≤5 m）→ 直接 /clear（必成功）。
2. 到 Ĝ 处 /clear：
   - success → 完成（本步仅移动+5 s）。
   - no_target_in_range → 说明 >20 m，就地补测：
       在当前点及沿垂直方向 ~20–50 m 处各测一次（短程，误差绝对值极小）
       → 精确交会更新 Ĝ' → 移动 → /clear，通常一两次内成功。
```
短程测量因 r 很小（几十米），±1° 的横向误差仅厘米级 → 快速收敛。

### 5.5 路径规划（近似 TSP）
- 有了各源粗位置后，"访问并清除全部"是一个带传感的 **TSP**。用**最近邻**构造初始回路 + **2-opt** 改进；起点 (0,0)，可不返回。
- 发现扫描的 7 个观测点顺序也并入整体路线一起优化（扫描点与清除点交织，边扫边清）。
- 由于移动是主成本，路线优化对总时间影响最大。

### 5.6 频道/状态管理与终止判据
- 维护每个频道状态：`未知 / 已发现(含示向度集合) / 已清除`。**只有 `/clear` 返回 success 才算已清除**。
- 后续扫描/测量**跳过已清除频道**，节省切换与检测时间。
- **终止**：完成全部覆盖点扫描且所有"已发现"频道均已清除后，做一次**中心+边缘确认扫描**；若无新信号，则认为清空（10–16 未知，故靠覆盖完整性保证不漏）。
- 稳健性：`no_signal` 对全向源意味着"该频道已清或超接收范围"，配合覆盖点设计基本不漏；对个别远边缘源若某点看不到，会被相邻覆盖点看到。

### 5.7 统计与实测
- 程序自记录指令序列/响应，自算：被清除个数、定位清除总虚拟时间 → 输出**被清除比例**与**平均定位清除时间**。
- 先在**演练测试**（结果会给出干扰源真数）反复调参验证，再做 **3 次正式测试**，按表 1 填 (案例编码, 清除个数, 平均时间, 程序运行时间)，导出正式日志入支撑材料。

---

## 6. 问题 4：全向 + 定向混合

在问题 3 框架上增量处理"定向源"带来的三个新困难。

### 6.1 新困难
1. **可能测不到**：定向源仅在其 180° 半平面内可测。某点 `no_signal` 变成**多义**：该频道已清 / 超接收范围 / **是定向源且你在其盲区**。→ 不能凭一次 no_signal 判定"无源"。
2. **发现更难**：需要从**源的辐射一侧**观测才能发现 → 单重覆盖不够，要多方位。
3. **计数更难**：10–16 未知，定向源可能长期沉默 → 要尽量提高被发现率以保证比例=1。

### 6.2 检测策略（多方位覆盖 + 不轻易否定）
- 观测点从问题 3 的 7 点**加密**为更多方位（如中心 + 内外两环、错开角度），使任一源周围有**分布在不同方向**的观测点，从而至少一个落在其辐射半平面内。
- 维护"频道 = 定向嫌疑"：某频道在一处曾有示向度、移动后转 `no_signal` 且尚未清除 → 判为**离开了其覆盖角**，回到"曾探测到"的方位继续接近，而非放弃。
- 对**从未探测到**的频道：这些频道要么无源、要么定向背对所有已访问点 → **继续增补不同方位的扫描点**（尤其绕到区域另一侧）直到发现或达成充分覆盖。计数逼近 16 时可提前收尾。

### 6.3 定位与清除
- 一旦收到示向度，其含义与全向相同（覆盖角内示向度不受方向性影响）→ **沿用问题 3 的最小二乘交会 + 20 m Endgame**。
- **清除不需要知道朝向**（20 m 内即可清）→ Endgame 完全复用。

### 6.4 （可选）方向与类型判别，增强论文完整性
- **朝向估计**：定位后绕源等半径采样检测，"有信号/无信号"的边界方向 ⟂ 定向轴 → 覆盖区中点即定向方向（两条边界方向的角平分线）。
- **全向/定向判别**：能从**四周各方向**都收到 → 判全向；只在**约半平面**收到（对侧在接收范围内却 no_signal）→ 判定向。距离不确定会干扰判别，作**启发式**给出并说明局限。

### 6.5 实测
同问题 3：演练调参（会给出总数/全向数/定向数）→ 3 次正式测试 → 填表 + 导出日志。

---

## 7. 与模拟器交互的软件架构（问题 3/4 需要）

```
robotdog/
├─ sim_client        HTTP+JSON 客户端：/enter /measure /clear /exit
│                    · request_id 幂等（新动作新 id；网络重试复用原 id 与原体）
│                    · 同时判 HTTP 状态 + accepted；处理"连不上"与"收到 JSON"两类
│                    · 超时/重连/退避；记录 virtual_time_s（仅取 accepted=true）
├─ world_state       机器狗当前位置、测向机当前频道、虚拟钟、各频道状态、示向度库、已清集合
├─ estimator         楔形交(问题1) + 加权最小二乘三角定位(问题3) + 不确定度评估
├─ planner           覆盖点生成、发现扫描调度、TSP(最近邻+2-opt)、Endgame 决策
├─ strategy_p3 / p4  两问的顶层状态机（发现→定位→排线→逐个清除→确认→exit）
├─ mock_sim          本地离线模拟器：按 §2.4 代价模型 + §1 物理规则复现，
│                    随机布置 10–16 源（P4 含定向），用于**无限次离线自测**与调参
└─ logger/metrics    自记录指令与响应；计算被清除比例、平均定位清除时间；导出
```
- **为何要 `mock_sim`**：正式测试每问仅 3 次、演练虽不限次但也要联网且有 9/13 截止。先用本地离线模拟器把策略跑到稳、参数调好，再上真实模拟器，避免浪费。离线模拟器要与官方计时/物理规则**逐条对齐**（含 near/no_signal/±1° 固定偏差/接收半径/定向半平面）。
- **鲁棒要点**：串行不并发；`accepted=false` 不推进时间且 `virtual_time_s=0` 不可当时刻；用 `remaining_real_duration_s` 控现实时长；避免异常高频循环（日志 2 MB 上限）。

---

## 8. 测试与评估方案

1. **离线阶段**：`mock_sim` 随机生成大量案例（覆盖 10–16 源、不同接收半径、P4 不同定向配置），跑策略统计**比例分布**与**平均时间分布**，做参数（覆盖点数/位置、补测阈值、TSP 改进轮数、Endgame 步长）敏感性分析。
2. **演练阶段**：接真实模拟器演练（不限次，结束给真数）核对离线一致性，修正物理/计时偏差，最终定策略。
3. **正式阶段**：各问 3 次正式测试，按**表 1**记录 (测试案例编码, 清除干扰源个数, 平均定位清除时间, 程序运行时间)，导出加密正式日志放入支撑材料（注意 9/13 15:30 前尽早完成）。
4. **指标**：主指标 = 被清除比例（目标 1.0）与平均定位清除时间；辅以总虚拟时间、检测次数、总移动距离、每源平均测量次数等做分析。

---

## 9. 假设、风险与开放问题

- **假设**：模型误差严格 ±1°；接收半径 ∈[1000,1500]；定向覆盖恰为 180° 半平面；示向度在覆盖角内与朝向无关；忽略高度差（题目已给）。
- **风险/待验证**：
  1. 误差是"每地点固定偏差"→ **原地重复测量无用**，务必用不同点（已在策略中体现）。
  2. 远边缘 + 小接收半径的源可能被单重覆盖漏掉 → 需验证 7 点覆盖是否足够，必要时加密。
  3. 定向源发现率是 P4 的比例=1 主要风险 → 多方位覆盖 + "不轻易否定" + 计数逼近 16 收尾。
  4. TSP 与"边走边定位"耦合（先有位置才好排线，先走才能测位置）→ 采用**粗定位→排线→就地精定位**的迭代闭环缓解。
  5. `no_signal` 多义性（P4）→ 严格以"仅 /clear success 记为已清"避免误判漏清。
- **开放问题**：覆盖点最优个数/位置的严格证明；有界系统偏差下最小二乘的偏差—方差权衡；发现率与观测点集合的关系（可作论文亮点）。

---

## 10. 计划产出物

- **问题 1**：半平面交 + 直径 + 直径圆覆盖判定的算法与证明，配图与反例。
- **问题 2**：误差—交会角分析、候选区域显式描述与图、推荐点构造。
- **问题 3/4**：完整策略与算法（伪代码/流程图）、复杂度、模拟器实测三次正式结果（表 1）、日志。
- **公共**：统一数学模型、离线模拟器、指标统计、参数分析。

> 下一步：先实现 `estimator`（问题 1/2 可独立成图验证），再搭 `mock_sim` + `sim_client`，最后做 P3/P4 顶层策略与实测调参。
